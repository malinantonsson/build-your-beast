import AddToCanvas from './modules/add-to-canvas';

/**
 * List all compontents that are initialised
 * using the data-behavior attr within this object
 * @type {Object}
 */
let importedComponents = {
    addToCanvas
}